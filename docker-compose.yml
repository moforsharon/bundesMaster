version: '3.9'

services:
  nextjs:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "30009:30009"
    environment:
      NODE_ENV: production
    env_file:
      - .env
    volumes:
      - /app/node_modules
